<!-- <fest:template xmlns:fest="http://fest.mail.ru" context_name="LoginErrors"> -->
<fest:template xmlns:fest="http://fest.mail.ru" >
   <div class="bs-panel panel"> 
     <form class="form-login" role="form">
       <h2 class="form-login__heading">Login</h2>
         <input class="form-login__control" placeholder="Login" required="" autofocus="" type="login" name="login" id="loginError"  data-container="body" data-toggle="popover"/>
         <input class="form-login__control" id="passwordError" placeholder="Password" required="" type="password" />
        <button class="form-login__btn" id="submitLogin" type="submit">Enter</button>

<!--         <fest:if test="!SignupErrors.NoErrors">
          <div class="form-login__validate-messages">
                  <fest:if test="LoginErrors.LoginError">
                      <p>Неверный логин</p>
                  </fest:if>
                  <fest:if test="LoginErrors.PasswordError">
                      <p>Неверный пароль</p>
                  </fest:if>
          </div>
        </fest:if> -->

<!--          <fest:if test="SignupErrors.NoErrors"> -->
          <div class="form-login__validate-messages_hide">
          </div>
<!--         </fest:if> -->
        <a href="#signup" class="form-login__btn">Create new</a>
     </form>

     <div class="menu menu_for-back">
        <a href="#main"class="menu__btn_short"> Back</a>
     </div>

    </div>
</fest:template>
